<h1>Editing Listing Provision</h1>

<%= render 'form', listing_provision: @listing_provision %>

<%= link_to 'Show', [@listing, @listing_provision] %> |
<%= link_to 'Back', listing_listing_provisions_path(@listing) %>
